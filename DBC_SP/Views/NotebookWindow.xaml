<Window x:Class="DBC_SP.Views.NotebookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DBC_SP.Views"
        mc:Ignorable="d"
        Title="Editor Notebooku" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Výrobce:" Grid.Row="0"/>
        <TextBox Text="{Binding Vyrobce}" Grid.Column="1" Margin="0,5"/>

        <Label Content="Model:" Grid.Row="1"/>
        <TextBox Text="{Binding Model}" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

        <Label Content="CPU:" Grid.Row="2"/>
        <TextBox Text="{Binding CPU}" Grid.Row="2" Grid.Column="1" Margin="0,5"/>

        <Label Content="RAM (GB):" Grid.Row="3"/>
        <TextBox Text="{Binding RAM_GB}" Grid.Row="3" Grid.Column="1" Margin="0,5"/>

        <Label Content="HDD (GB):" Grid.Row="4"/>
        <TextBox Text="{Binding KapacitaUloziste_GB}" Grid.Row="4" Grid.Column="1" Margin="0,5"/>

        <Label Content="Cena:" Grid.Row="5"/>
        <TextBox Text="{Binding Cena}" Grid.Row="5" Grid.Column="1" Margin="0,5"/>

        <Label Content="Kusů skladem:" Grid.Row="6"/>
        <TextBox Text="{Binding PocetKusuSkladem}" Grid.Row="6" Grid.Column="1" Margin="0,5"/>

        <Label Content="Detail:" Grid.Row="7"/>
        <TextBox Text="{Binding Detail}" Grid.Row="7" Grid.Column="1" Margin="0,5"/>

        <Button Content="✅ Uložit" Click="BtnSave_Click" IsDefault="True" 
                Grid.Row="9" Grid.Column="1" Height="30"/>
    </Grid>
</Window>
